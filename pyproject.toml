[project]
name = "fastapi-homework"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.141.1",
    "unicorn>=2.1.4",
    "uvicorn[standard]>=0.52.4",
]

[project.scripts]
fastapi-homework = "fastapi_homework:main"

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"
